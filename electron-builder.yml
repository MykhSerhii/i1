appId: com.ukraine.map
productName: Карта висот України

directories:
  output: dist-electron

# Файли що потрапляють в .exe (без data/ — вона лежить поруч з .exe)
files:
  - "**/*"
  - "!data/**"
  - "!scripts/**"
  - "!src/**"
  - "!tools/**"
  - "!logs/**"
  - "!.git/**"
  - "!dev.sh"
  - "!update.sh"
  - "!compile.sh"
  - "!README.md"

# Шрифти вкладаємо в resources/ всередині .exe
extraResources:
  - from: public/
    to: public/

win:
  target:
    - target: portable
      arch: [x64]
  artifactName: map.exe
  # Відключаємо code signing — не потрібен для особистого використання
  # Без цього electron-builder намагається розпакувати winCodeSign
  # що вимагає прав адміністратора для створення symlinks
  sign: null
  signingHashAlgorithms: null
  # Іконка (необов'язкова — якщо є build/icon.ico)
  # icon: build/icon.ico

portable:
  artifactName: map.exe
