{
  "name": "ukraine-map",
  "version": "1.0.0",
  "description": "Офлайн карта висот України",
  "main": "main.js",
  "scripts": {
    "postinstall": "node scripts/download_fonts.js",
    "build": "node scripts/build.js",
    "start": "npm run build && electron .",
    "dist": "npm run build && electron-builder --win --x64"
  },
  "dependencies": {
    "express": "^4.18.2",
    "maplibre-gl": "^4.7.1",
    "pmtiles": "^3.2.0"
  },
  "devDependencies": {
    "electron": "^33.0.0",
    "electron-builder": "^25.0.0",
    "esbuild": "^0.24.0"
  }
}
